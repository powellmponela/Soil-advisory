let currentArea=null,currentPath='';const list=document.getElementById('private-files'),title=document.getElementById('file-title'),up=document.getElementById('up');function formatBytes(n){if(n==null)return'';if(n<1024)return n+' B';if(n<1048576)return(n/1024).toFixed(1)+' KB';return(n/1048576).toFixed(1)+' MB'}async function loadArea(area,path=''){currentArea=area;currentPath=path;const r=await fetch(`/api/staff/list/${area}?path=${encodeURIComponent(path)}`);if(r.status===401){window.location='/staff/login';return}const p=await r.json();title.textContent=area.toUpperCase()+(path?' / '+path:'');up.disabled=!path;list.innerHTML='';if(!p.entries.length){list.textContent='No files found at this location.';return}for(const e of p.entries){const row=document.createElement('div');row.className=`file-row ${e.type}`;row.textContent=(e.type==='folder'?'📁 ':'📄 ')+e.name+(e.size_bytes==null?'':'  '+formatBytes(e.size_bytes));if(e.type==='folder')row.addEventListener('click',()=>loadArea(area,e.relative_path));list.appendChild(row)}}document.querySelectorAll('.area').forEach(b=>b.addEventListener('click',()=>loadArea(b.dataset.area)));up.addEventListener('click',()=>{const parts=currentPath.split('/').filter(Boolean);parts.pop();loadArea(currentArea,parts.join('/'))});